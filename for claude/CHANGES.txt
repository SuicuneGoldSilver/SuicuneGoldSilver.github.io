POKEREF - CHANGES LOG
=====================

Session: 2026-05-19
-------------------

DATA - cj SET (サイバージャッジ / Cyber Judge)
- Added cj set to sets.json (28 total sets)
- Created public/data/cards/cj.json with 71 cards (JP + EN fields fully populated)
- Added 71 entries to index.json (1471 → 1542)
- JP data: all 71 cards read from cj/jp/ images
  Types: Grass (1-16), Fire (17-22), Lightning (23-30), Psychic (31-36),
         Fighting (37-41), Psychic ex (42), Metal (43-51), Lightning Future (52),
         Colorless (53-60), Trainers (61-70), Special Energy (71)
- EN data: all 71 EN images at cj/en/ matched JP order exactly (no reordering needed)
  English set: Temporal Forces (setIdEn: "tf", 162 cards)
- Corrections applied:
  - テツノイサハex (#1011), テツノカシラex (#1018), テツノイワオex (#1021): dex numbers filled
  - リキキリンex: dex #982 (Farigiraf)
  - All ex Pokémon textJa: "ポケモンexがきぜつしたとき、相手はサイドを2枚とる。"
  - ブーストエナジー 未来 (card 65): regulationMark set to "G" (reprint)
  - ミストエネルギー: subtypes: ["Special"]
  - No "Steel" / "Dark" / "Pokémon Tool" / "ACE SPEC" normalization errors

Session: 2026-05-16 (continued)
--------------------------------

HOMEPAGE - TEXT UPDATES
- Updated "セットを閲覧" feature card description:
    "今のレギュレーションのカードをすべて収録しています。セット名または全セットで見れます。"
- Updated "名前で検索" feature card description:
    "日本語・英語どちらのカード名でも検索できます。セット内の検索または全セット一括検索できます。"

HOMEPAGE - NAV
- Added nav links to the right of the SuicuneGS logo: カード検索 and 日英単語リスト
- Nav is now a flex row; links styled in muted text with hover-to-white transition

NEW PAGE - 日英単語リスト (/vocablist.html)
- Created vocablist.html in project root; added to vite.config.ts as a named build input
- Ported all 10 vocabulary tables from the old SuicuneGoldSilver.github.io/guide1.html:
    カードの種類, 特殊状態, ポケモンのタイプ, 対戦に使うもの, 場所の名前,
    対戦が始まる前, コインを投げること, 対戦でよく使う表現,
    相手の間違いを指摘すること, 試合の後
- Rebuilt with the site's dark theme (no Bootstrap dependency)
- Audio buttons use the browser's Audio API; mp3 files should be placed in public/audio/
- Audio button styled as a subtle dimmed speaker SVG icon (brightens to accent on hover)

Session: 2026-05-16
--------------------

UI - SET SELECTOR
- Set pills now display the set logo image (images/sets/{id}/logo.png) alongside the name
- Pills use flex layout with logo on the left; logo hides gracefully if the image is missing

UI - CARD GRID
- Each card thumbnail now has a 3px colored bottom border based on the card's primary type:
    Grass=green, Fire=orange-red, Water=blue, Lightning=gold, Psychic=purple,
    Fighting=brown, Darkness=indigo, Metal=silver, Dragon=deep-blue, Colorless=gray
  Trainer/Energy cards with no type use the default border color
- The type color also drives the border glow on hover (fixes leftover orange glow
  rgba(232,100,58) that no longer matched the accent palette)

UI - CARD MODAL
- Added ‹ › arrow buttons for desktop navigation (hidden on mobile ≤600px — swipe still works)
  Arrows sit outside the modal panel as flex siblings in the backdrop (gap: 16px each side),
  so they never overlap modal content. Both are always in the DOM at fixed size (visibility:hidden
  when at first/last card) so the panel stays perfectly centered while navigating.
- Footer now shows a position counter: e.g. "47 / 102"
- Fixed image hover glow: was hardcoded orange, now matches --color-accent

UI - SEARCH BAR
- Added a search icon (SVG magnifying glass) inside the left edge of the input
  Icon turns accent-blue when the field is focused

UI - HEADER
- Made header more compact: title and subtitle sit on one line (flex row, baseline-aligned)
- Added a very subtle top-to-transparent accent gradient as a background wash

Session: 2026-05-04
--------------------

VISUAL / THEME
--------------
- Changed accent color from burnt orange (#e8643a) to Suicune-inspired pastel sky blue (#a8d4e8)
- Updated all elements that use --color-accent (site title, card hover borders, active tabs, stat labels)
- Fixed text contrast on accent-colored backgrounds: switched from white (#fff) to dark navy (#0f1117)
  for active language tab, regulation mark tag, and active set pill
- Changed set name tag color from green (#2a3a2a / #a8c8a8) to Suicune purple (#2d2a3a / #c4b0e8)

MOBILE LAYOUT
-------------
- Card grid: added media query (max-width: 480px) to force 3 columns with 8px gap
- Card modal image: increased size from 120px fixed to 80% width (max 300px) and stacked
  the JP/EN toggle underneath the image instead of beside it
- Card modal: added swipe-down drag handle pill at the top of the modal

CARD MODAL - FEATURES
---------------------
- Added pronunciation button (speaker icon) next to the English card name
  Uses the browser's built-in Web Speech API (window.speechSynthesis) — no external dependencies
  Only visible in EN view
- Changed free retreat cost display from "なし" / "Free" to "—" in both languages

CARD MODAL - SWIPE GESTURES
----------------------------
- Swipe down to close: detects downward swipe (>80px) on the modal panel
  The details section (card info) is excluded — only the image area triggers close
- Swipe left/right to navigate cards: detects horizontal swipe (>60px)
  Direction is determined by which axis (X or Y) is dominant, preventing conflicts
  Left = next card, Right = previous card
  Disabled at first/last card automatically

CARD MODAL - ANIMATIONS
------------------------
- Swipe left/right: exit animation slides content out (translateX ±100%), enter animation
  slides new content in from opposite side (translateX → 0)
  Animated on inner modal-content wrapper (NOT the panel) to prevent flicker —
  panel's overflow:hidden clips the sliding content cleanly
- Navigation preserves the current JP/EN language view (does not reset to JP on swipe)
- Swipe down to close: panel slides down (translateY 100vh) + backdrop fades out over 0.42s
- Modal open: panel slides up from bottom (translateY 100vh → 0) over 0.38s

DATA - gotr SET
---------------
- Fixed imageEn: all 98 cards in the gotr set had imageEn: null despite EN images existing
  in public/images/sets/gotr/en/. Set imageEn to "images/sets/gotr/en/{n}.jpg" for all cards.
- Recovered from a PowerShell encoding corruption (UTF-8 read as CP1252 then re-written)
  using a Node.js reverse-mapping script
- Populated card data for 92/98 cards using the Pokémon TCG API (set ID: sv10 / Destined Rivals):
  attacks (name, cost, damage, text), abilities, weaknesses, resistances, retreatCost, artist
  6 cards had no EN equivalent and remain empty:
    Flapple, Team Rocket's Snom, Team Rocket's Revaroom,
    Team Rocket's Safari Ball, Team Rocket's Surprise Bomb, Team Rocket's Receiver
- Japanese attack/ability names (nameJa, textJa) left as null — need manual entry from JP cards
- EN card numbers corrected (set has 244 cards total, not 182 as previously recorded)
